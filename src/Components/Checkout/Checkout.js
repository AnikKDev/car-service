import React from 'react';
import { useLocation, useNavigate } from 'react-router-dom';

const Checkout = () => {

    

    return (
        <div>
            <h1>Checkout here</h1>
        </div>
    );
};

export default Checkout;